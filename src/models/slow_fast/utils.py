# import torch


# def make_samples(batch, history_size):
#     """
#     Args:
#         batch (torch.tensor): batch of data with shapes
#             (batch)x( 2*(history_size + 1) + 3 )x(height)x(width)
#         history_size (int): history size

#     Returns:
#         4 dimensional tensor with shapes:
#             (batch)x(channels)x(time)x(height)x(width)
#     """

